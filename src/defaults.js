const defaults = {
    glowRadius: 50,
    bgColor: null,
    flareColor: 'rgba(254, 1, 154, 0.5)',
    fluentFlares: false,
    onDestroy: (self) => {},
    onReset: (self) => {},
    onStop: (self) => {},
    onStart: (self) => {}
}

export default defaults
